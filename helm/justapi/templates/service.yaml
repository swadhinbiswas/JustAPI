apiVersion: v1
kind: Service
metadata:
  name: {{ include "justapi.fullname" . }}
  labels:
    {{- include "justapi.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "justapi.selectorLabels" . | nindent 4 }}
